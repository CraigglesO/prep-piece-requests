{
  "name": "prep-piece-requests",
  "version": "0.9.0",
  "description": "Torrents have different pieces sizes. This will manage those differences",
  "main": "prep-piece-requests.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "start": "npm run build:live",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./prep-piece-requests.ts"
  },
  "author": "Craig OConnor",
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/CraigglesO/prep-piece-requests.git"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^7.0.0",
    "nodemon": "^1.11.0",
    "ts-node": "^2.0.0",
    "typescript": "^2.1.5"
  }
}
